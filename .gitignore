node_modules/
data/

# Autoresearch experiment ledger — append-only, not tracked (see docs/research-schema.md §7)
research/results.tsv

# Session credentials — never commit
auth/

# Local scratch / exploration scripts (contain hardcoded credentials)
test-jq-login.js
test-jq-editor.js
__pycache__/
research/normalize-*.tsv

# Autoresearch team idea queue — transient working state, not tracked
research/ideas-queue.json

# Autoresearch team loop checkpoint — transient resume state, not tracked
research/loop-state.json

# Auto-study transient state (queue + findings ledger), not tracked
study/*/findings.tsv
study/*/questions.json
